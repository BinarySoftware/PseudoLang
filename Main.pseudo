Gcd(a,b)
    // still to fix
    if(a=0)
        then return b
    r <- b mod a
    return Gcd(r, a)

// FIXME 1 -  there's need to put line in between nested functions declaration
// FIXME 2 -  parse condition of if, while, do, repeat, for