Gcd(a,b)
    // 1
    if(a=0)
        then c = b * 2 / 2
        return c
    r <-- b mod a
    return Gcd(r, a)

// FIXME 1 -  there's need to put line in between nested functions declaration

if(a<b)
    then c=a-2
         sdfnosfsif
    else if(a<b)
         then c=a-2
              sdfinaf
         else c=b+2

